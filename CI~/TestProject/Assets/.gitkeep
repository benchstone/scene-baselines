Unity refuses a project whose Assets folder is missing, and git cannot store an
empty directory. This placeholder keeps the folder in the checkout, so CI gets a
project Unity will actually open. The tests live in the package, not here.
